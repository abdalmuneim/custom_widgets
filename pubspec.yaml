name: custom_widgets_2
description: This package contains some widgets that are indispensable in any project and are intended for ease of handling.
version: 0.0.1
homepage: https://github.com/abdalmuneim/custom_widgets_2.git

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.3.1
  flutter_svg: ^2.0.10+1
  image_picker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
